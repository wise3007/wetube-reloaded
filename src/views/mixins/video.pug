//- mixin video(info) <-- 안에 받을 인자는 똑같지 않아도 됨
mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
            p=video.description
            ul
                each hashtags in video.hashtags
                    li=hashtags
            small=video.createdAt
            hr