extends base
include mixins/video
    
block content
    form(method="Get")
        input(type="text", placeholder="search", name="keyword")
        input(type="submit", vlaue="search now")
    div
        each video in videos
            +video(video)