extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="username", type="text", required, placeholder="아이디")
        input(name="password", type="password", required, placeholder="비밀번호")
        input(type="submit", value="Login")
        br
        a(href="/users/github/start") github로 로그인하기 &rarr;
        a(href="/users/kakao/start") 카카오 로그인하기 &rarr;
    hr
    div
        span 계정이 없으신가요? 
        a(href="/join") 회원가입  &rarr;